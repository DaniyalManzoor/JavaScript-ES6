// Higher order function

// Higher order function are the function that take a
// function as an argument or return it or both.

let numbers = [1, 2, 3];
let product = numbers.map((number) => number * 2);

//after one second
setTimeout(() => console.log("Hello world"), 1000);
